The "primers" sequences in the fasta files in this directory do not exactly correspond to the actual primer sequences used in the RT and DNAPol experiments, but instead correspond to sequences that enable the correct parsing of the paired reads.
